## Ejercicio 1: La Caja de Herramientas Matemáticas

""" Objetivo: Crear un módulo simple e importarlo.

1.  Crea un archivo llamado `operaciones.py`.
2.  Define tres funciones dentro de él:
      * Una que sume dos números.
      * Una que reste dos números.
      * Una que calcule el cuadrado de un número.
3.  Crea un segundo archivo en la misma carpeta llamado `programa_principal.py`.
4.  Importa el módulo `operaciones` dentro de `programa_principal.py`.
5.  Solicita al usuario dos números y utiliza las funciones del módulo importado para mostrar la suma, la resta y el cuadrado del primer número. """

## Ejercicio 2: El Importador Selectivo y Alias

""" Objetivo: Practicar `from ... import ...` y `as`.

1.  Crea un módulo llamado `conversor_texto.py`.
2.  Define dos funciones: `convertir_mayusculas(texto)` y `contar_letras(texto)`.
3.  Crea un archivo llamado `app_texto.py`.
4.  Instrucción específica:
      * Importa solo la función `convertir_mayusculas` directamente (sin traer todo el módulo).
      * Importa la función `contar_letras` pero asígnale el alias (renómbrala) como `contador`.
5.  Usa ambas funciones en el archivo principal con una frase de prueba. """